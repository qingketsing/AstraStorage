Leader和leader写错了。。。。

window测试环境下TCP数量限制，无法进行大量（>300）的TCP连接测试,目前三百可以正常通过测试

RPC通信层，然后继承RaftRPC，创建了集成层，然后应用

discovery management注册raft rpc通信

服务端口冲突

使用 Start-Job 创建后台任务，这些任务只在启动它的PowerShell会话中可见，当你在另一个PowerShell窗口测试时，看不到这些后台任务，节点实际没有运行

docker部署测试时的go的版本问题，目前只能1.23

接口的临时增加导致频繁的修改细节，比如上传文件时，一开始没有上传Client自身的IP，导致了leader node节点没法进行TCP连接，后来加入了这个参数，发现传递过程中也会有一些问题，比如怎么存储，怎么调用这个参数以及什么时候调用，还有TCP的连接什么时候该连接，什么时候该断开。