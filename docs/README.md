# Documentation Map

This directory holds project documentation.

## Layout

- `architecture/`: current architecture, deployment, testing, and system behavior documents
- `superpowers/`: planning and spec artifacts produced during implementation sessions
- `deploy/`: reserved for future deployment runbooks and operations-specific docs

## Where To Start

For current project understanding, start with `architecture/`.

For historical planning context, use `superpowers/`.

For a delivery-oriented summary of startup steps, demo scope, and current boundaries, read [poc.md](/home/qingke/AstraStorage/docs/poc.md).
