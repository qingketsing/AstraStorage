global:
  resolve_timeout: 5m

route:
  receiver: local-dev
  group_by:
    - alertname
    - job
    - instance
  group_wait: 10s
  group_interval: 1m
  repeat_interval: 1h

receivers:
  - name: local-dev
