apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: astra-mds
  namespace: astrastorage
  labels:
    app.kubernetes.io/name: astra-mds
    app.kubernetes.io/part-of: astrastorage
    release: kube-prometheus-stack
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: astra-mds
      app.kubernetes.io/part-of: astrastorage
  endpoints:
    - port: http
      path: /metrics
      interval: 15s
      scrapeTimeout: 5s
