# Deployment Assets

This directory holds runnable deployment assets for AstraStorage.

## Layout

- `docker/`: local development stacks and image build assets
- `k8s/`: Kubernetes manifests organized by component

## Use

Use `docker/` when you want a local compose-style environment or local images.

Use `k8s/` when you want in-cluster deployment manifests and monitoring resources.
